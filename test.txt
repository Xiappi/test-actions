
/* CONNECT "" USER "" PASSWORD ""; */
SET TERM ^;

create procedure sps_model_dwelling_options_get_occ_ids (
    in_state     varchar(2) character set none,
    in_line      varchar(3) character set none,
    in_rate_date date,
    in_filter    varchar(100) character set none)
returns (
    out_filter      varchar(100) character set none,
    out_reftable_id integer)
as
    declare variable filter_count integer;
begin
suspend;
end
^



INSERT INTO applied_scripts(NAME, DESCRIPTION, SCRIPT_DATE)
VALUES ("171031TN1", "proc used to lookup occ codes", "10/31/2017")^
SET TERM ;^
COMMIT WORK;
